---
name: test-defect-retrospective
description: 测试文档评审与缺陷复盘工具包：交互式问卷收集环境信息，按输入目录自动路由——有缺陷导出(Jira/禅道)即产出产品/开发/测试三类复盘报告(Markdown+JSON+汇总HTML)，无缺陷导出则支持需求评审、测试用例评审、技术方案评审三种定性模式。当用户要求做版本缺陷复盘、测试总结、缺陷分析、测试效能分析或需求/用例/技术方案评审时调用。
---

# 测试缺陷复盘与文档评审 Skill

纯文件处理：本地 Python 脚本，无 Web/服务端/数据库。命令默认在本 Skill 目录执行；Python 3.8+，Excel 需可选依赖 `openpyxl`（CSV 无依赖）。

## 模式路由（按目录内容自动判定）

| defects/ | requirements/ | test_cases/ | tech_designs/ | 模式 |
|---|---|---|---|---|
| 有 | 任意 | 任意 | 任意 | **复盘模式**（量化管线；tech_designs 有文件时追加技术评审） |
| 无 | 有/- | -/有 | -/有 | **定性评审**（各目录各出一份评审报告，可组合） |
| 全空 | | | | 提示无可分析素材 |

## 一、启动流程

### 1. 断点检查
`python scripts/cli.py session show`——`answers_complete: true` 则跳过问卷直接初始化；否则从首个未答题继续。

### 2. 交互式问卷（AskUserQuestion，每答一题立即落盘）

只问 4 题，文件格式不设问（解析器按扩展名与表头自适应）：

| 序 | 问题（key） | 选项 | 作用 |
|---|---|---|---|
| Q1 | 缺陷数据平台？（defect_platform） | Jira / 禅道 / 其他 | 决定解析插件；无导出文件可先用 `scripts/jira_fetch.py` PAT 拉取（附录B）。选「其他」→ 追问格式与字段含义，生成 `scripts/custom_field_map.json` 后按 `custom` 保存 |
| Q2 | 发布时间？（release_time） | `YYYY-MM-DD HH:MM` / 不清楚 | 遗留缺陷判定基准；不清楚则 analyze 省略 `--release-time`，以缺陷最大创建时间近似并标注 |
| Q3 | 上版复盘数据在哪？（has_prev_report） | 同目录（自动探测）/ 其他位置（追问路径）/ 没有 | 版本环比与规律性反复缺陷依据；没有则环比标注缺失 |
| Q4 | 人员角色.csv？（team_roles） | 已准备 / 不提供 | 角色口径；不提供则按「提报人=测试、解决人=开发」近似并警示 |

落盘：`python scripts/cli.py session set --answer defect_platform=jira`（其余 key 同理）。

### 3. 目录初始化
确认项目名/版本号后 `python scripts/cli.py init --root "{项目名}/{版本号}"`：

```
{项目名}/{版本号}/
├── defects/       # 【可选·复盘触发】缺陷导出（CSV/Excel）；明细表放 defects/明细/
├── requirements/  # 【可选】需求文档（docx/md/txt）
├── test_cases/    # 【可选】用例（CSV/Excel/XMind）
├── tech_designs/  # 【可选】技术方案（docx/md/txt）
├── 人员角色.csv   # 【可选】姓名,角色（前端/后端/产品/测试）
└── reports/       # 【输出】
```

- **人员角色.csv**：提供后仅「测试」计入提报/验证效能、「前端/后端」计入修复效能，混入角色剔除并标注；未登记人员计入并提示补填。
- 格式自适应：.doc 实为 MHTML 时先用 `scripts/mht_extract.py` 提取；图片/在线链接不可读按缺失标注；XMind 约定（中心主题=套件、中间层=模块、叶子=用例、优先级图标→P0~P3）；Jira API 自定义表头配合 custom_field_map.json 走 custom 解析。

## 二、执行流程（复盘模式）

1. **scan**：`cli.py scan --root …` → 缺失清单 + `mode`（retrospective/review）。
2. **parse**：`cli.py parse --root … [--platform auto|jira|zentao|custom]` → 标准中间数据集 JSON。
3. **analyze**：`cli.py analyze --root … [--release-time …] [--prev-root …]` → 三份报告+指标全量 JSON（带时间戳，不覆盖历史、不改输入）。
   - **用例双口径**：①需求-用例关联分析（常驻）：文档名↔用例路径段名称匹配（归一化后包含或相似度≥0.45），输出漏配清单；②缺陷-用例关联类指标（显隐）：编号关联可 join 才出双向覆盖表，用例带执行状态才出执行覆盖率/阻塞/回归/模块缺口——不满足时整节隐藏（不以 None/0%/「未执行」展示）。
   - **产物命名**：`{系统名}_{迭代号}_{产物分类}_{ts}`（如 `物流管理系统_v1.6.0_复盘报告_产品_20260920_144615.md`）。
4. **html**：`cli.py html --root …` → 汇总仪表板（单文件自包含、内嵌指标 JSON、支持打印）；`--doc <md>` 可选渲染档案风 HTML。环比自动匹配：未指定 prev-root 时按版本号语义排序取上一版本。

### 3R 定性评审模式（mode=review 时替代第 2~4 步）

评审报告以 md 交付（HTML 可选）。文档读取统一：docx 用 `docx_extract.py`（含删除线识别）、md/txt 直读、MHTML 用 mht_extract.py。

| 类型 | 产物 | 评审框架 |
|---|---|---|
| 需求评审 | `{系统名}_{迭代号}_需求评审报告_{ts}.md` | 结构拆解 → 四维评估（逻辑自洽/重复赘述/文案可读性/提示语清晰度）→ 交互体验 → 澄清清单（P0/P1/P2，核心价值） |
| 用例评审 | `…_用例评审报告_{ts}.md` | 量化底稿：`analyzer.parse_testcases(目录)` 统计总数/分布/关联率；定性：结构完整性→覆盖设计→文案可判定性→优先级与回归标记→澄清清单 |
| 技术评审 | `…_技术评审报告_{ts}.md` | 完整性五要素（背景/目标/选型/影响面/灰度回滚）→ 风险识别 → 可测试性评估（核心）→ 与需求一致性 → 澄清清单 |

复盘模式下 tech_designs/ 有文件时追加技术评审（3R.4）。

### 第 4 步：AI 定性补充（只追加，不删改量化表格）

逐项：①Bug 描述风格分析（titles_sample）②需求四维评估 ③交互体验评估 ④高风险漏测清单（边界/并发/大数据量/弱网，结合 gap 与关键词扫描）⑤环境差异风险评估（无输入标注缺失）⑥接口/UI 分层建议（评分为脚本确定性计算）⑦用例设计/流程改进建议 ⑧可封装 Skill 清单 ⑨环比定性 + 上一轮改进项落地校验（读上版报告原文逐项核对）。

### 第 5 步：复核
无编造（缺失即标注）；复盘模式生成汇总 HTML；向用户报告产物路径清单。

## 评审判定规范（所有 AI 定性内容的执行标准）

**六条原则**：
1. **绝对锚点**：评级基于固定锚点（新成员/外部审计视角：无上下文能否仅凭素材得出同样结论）；环比仅作补充，禁止作为调级依据。
2. **证据先行**：每个评级附可复核证据（原文引用/条目ID/数字）；正向与负向评级同等证据强度；无证据的形容词删除。
3. **口径一致**：跨版本/跨人比较必须同口径并显式标注差异；未发布版本总量类结论标「过程读数，终版为准」。
4. **小样本降级**：个人级指标样本 <5 标注「小样本仅供参考」，禁止排名或下团队级结论。
5. **数据谦抑**：字段未启用导致的 0/None/100% 按缺失处理（隐藏或标注），禁止解释为真实值；数据不支撑的结论不写。
6. **负向优先校验**：生成前自问「最高/最低评级被质疑时证据是什么」，答不出立即降级或删除。

**自检清单（第 4 步收尾必做）**：评级均有锚点+证据？结论均含数据引用？环比未冒充绝对判断？小样本/口径/过程读数已标注？三份报告尾部各落自检声明（`> 本报告 AI 定性内容已按判定规范自检：评级 N 项均附证据锚定；小样本标注 M 处；相对比较仅作补充 K 处。`）？

## 三、全量分析指标口径（同输入必同输出；实现见 analyzer.py）

### 1. 缺陷大盘
| 指标 | 口径 |
|---|---|
| Bug总数 / 有效缺陷 | 去重后总数；有效 = 总数 − 无效（解决结果：不予解决/设计如此/无法重现/无效/重复/转需求） |
| Bug解决率 | 状态组=已关闭 ÷ 有效 × 100% |
| 严重程度 / 优先级 / 模块分布 | 分组统计数量与占比 |
| 重复缺陷 | 平台标记，或标题相似度≥80%（去空白标点 difflib）且同模块同级别 |
| 规律性反复缺陷 | （模块,根因）连续 ≥2 版本出现；无上版数据标注缺失 |
| 偶现缺陷 | 复现概率字段含偶现类词或 <30%；或标题/描述关键词兜底（偶现/偶发/不稳定/无法复现等） |
| 存量遗留 | 创建早于发布时间且未关闭；发布时间未知以最大创建时间近似并标注 |
| 根因统计 | 7 类关键词归类（需求/代码逻辑/环境配置/数据/设计缺陷/兼容性/操作失误），未归类记「未分类」 |

### 2. 人员效能
| 维度 | 指标与口径 |
|---|---|
| 测试（按提报人；验证人缺省取提报人） | 提交数/占比；首过率 = 重开=0 ÷ 验证总数；驳回重修复率 = 重开>0 ÷ 验证总数；平均验证时长 = mean(关闭−解决)；重复上报率；角色口径：有名单仅计「测试」（自提剔除并标注） |
| 开发（按解决人，缺省取经办人） | 平均修复时长 = mean(解决−分配，分配缺失回退创建)，按严重级别分层；回弹率 = 重开>0 ÷ 已修复；角色口径：有名单仅计「前端/后端」 |
| 生命周期 | 响应(分配−创建)/修复(解决−分配)/验证(关闭−解决) 平均时长 + 分桶（<4h/4-24h/1-3天/>3天） |

### 3. 版本环比（需上版报告，缺失整节标注）
核心指标同比（总数/解决率/重复率/偶现率/根因TOP/修复与验证时长/整体首过率/回弹率）；上一轮改进项落地校验；变化点/进步点/核心短板。匹配：同项目目录版本号语义排序取上一版。

### 4. 需求侧（需 requirements/）
四维评估（AI，按判定规范）；需求变更统计（`需求变更记录.csv` 存在时脚本统计，否则 AI 或标注缺失）；临时加急缺陷率（标签/标题含「加急/临时」÷ 总数）；依赖类缺陷清单（标题/备注含「联调/依赖/上游/下游/接口对接」）；产品交互体验评估（AI）。

### 5. 测试执行（需 test_cases/，按双口径显隐）
用例总数/状态分布/执行覆盖率（需状态字段）；需求-用例双向覆盖（需编号关联）；阻塞事件分类（环境/提测延期/依赖/数据/其他）；回归工作量（需回归与时长列）；漏测清单（关键词扫描+gap）；环境差异评估（无输入标注缺失）。

### 6. 测试资产优化
自动化优先级评分（确定性）：`0.3×业务核心度 + 0.2×回归频率 + 0.2×手工成本 + 0.2×历史缺陷密度 + 0.1×稳定性`（各维归一化 0~1；缺用例数据仅缺陷维降权）；P0=前20%/P1=次30%/P2=其余；≥0.6 高/≥0.35 中/其余低；接口/UI 分层为 AI 建议。其余（用例设计优化、可封装 Skill 清单等）为 AI 输出。

## 四、产物与章节（固定，不得增减）

复盘三报告（`_产品/_开发/_测试_{ts}.md/.json` + `_复盘数据_指标全量_{ts}.json` + `_复盘报告_汇总_{ts}.html`），脚本生成量化表格、AI 章节内追加：

- **产品**：①需求文档评审结论 ②模块缺陷与需求问题关联 ③需求变更与连锁缺陷 ④临时加急质量 ⑤上下游依赖缺失 ⑥需求引入类缺陷溯源 ⑦交互体验评估 ⑧历史遗留风险
- **开发**：①缺陷大盘 ②根因分类统计 ③偶现清单 ④个人修复时效（按严重级别分层）⑤回弹率 ⑥高频重复缺陷根因 ⑦流程优化建议
- **测试**：①提报效能 ②验证通过率与驳回率 ③版本环比 ④上轮改进项校验 ⑤用例与需求关联分析 ⑥阻塞事件 ⑦回归工作量 ⑧漏测风险清单 ⑨环境差异风险 ⑩用例设计短板 ⑪自动化优先级清单 ⑫可封装 Skill 清单 ⑬流程改进建议 ⑭重复上报率

骨架：报告头（生成时间/来源/输入文件/缺失素材）+ 各章节（量化表格 + 【AI分析】段落 + 缺失时 `⚠️ 【数据缺失】缺少XX，该项暂无法分析`）。注：根因依赖章节（开发②、产品②⑥）在根因字段未启用时整节隐藏。

评审模式产物：`{系统名}_{迭代号}_{需求|用例|技术}评审报告_{ts}.md`。

## 五、缺失数据处理（硬性）
禁止编造；scan 前置校验后执行可分析部分；缺失项章节末统一标注 `⚠️ 【数据缺失】…`；幂等运行（时间戳产物，不覆盖历史、不改输入）。

## 六、硬性约束
禁止 Web 平台/服务端/数据库代码（静态 HTML 报告除外）；Skill 加载自动触发问卷；输入路径 `{项目名}/{版本号}/`；量化口径统一（第三节）；不输出人员隐私、不跨版本合并数据；复盘模式必出汇总 HTML，评审模式 md 即最终产物。

## 附录A：标准字段（解析器输出，与平台解耦）

| 标准字段 | 含义 | Jira 表头 | 禅道表头 |
|---|---|---|---|
| bug_id / title / status | 编号/标题/状态 | Issue key / Summary / Status | ID / Bug标题 / 状态 |
| severity / priority | 严重程度/优先级 | Severity / Priority | 严重程度 / 优先级 |
| module / labels | 模块/标签 | Components / Labels | 模块 / 关键词 |
| reporter / assignee / resolver / verifier | 提报/经办/解决/验证人 | Reporter / Assignee / Resolver / Verifier | 创建者 / 指派给 / 解决者 / 关闭者 |
| created_at / assigned_at / resolved_at / closed_at | 四个时间戳 | Created / Assigned / Resolved / Closed | 创建/指派/解决/关闭日期 |
| resolution / root_cause / reopen_count | 解决结果/根因/重开 | Resolution / Root Cause / Reopen | 解决方案 / 根因 / 激活次数 |
| requirement_id / repro / remark / duplicate_of | 关联需求/复现概率/备注/重复关联 | 需求ID / 复现概率 / Description / Duplicate of | 关联需求 / 复现概率 / 复现步骤 / 重复关联 |

派生字段（脚本计算）：status_group、severity_std、priority_std、resolution_std、root_cause_std、is_occasional、is_duplicate_flag、*_dt。

## 附录B：CLI 参考

```
cli.py session show | set [--project X --version Y --answer k=v] | reset
cli.py init   --root "{项目名}/{版本号}"        # 创建 5 目录
cli.py scan   --root …                          # 校验+模式路由
cli.py parse  --root … [--platform auto|jira|zentao|custom]
cli.py analyze --root … [--release-time "…"] [--prev-root PATH]
cli.py all    --root …                          # scan+parse+analyze
cli.py html   --root … [--doc <md路径>]
docx_extract.py "<docx>" [-o out.txt]           # docx 提取（含删除线）
jira_fetch.py --base <地址> --pat <PAT|环境变量JIRA_PAT> --project X --sprint N --out "<root>/defects"   # 拉主表+明细（落 defects/明细/）+人员角色_模板
mht_extract.py <.doc/.mht 或目录> [-o 输出目录]  # MHTML → 纯文本
```

## 附录C：custom_field_map.json（Q1 选「其他」时 AI 生成）

原始表头 → 标准字段（取值见附录A）：`{"工单号":"bug_id","问题主题":"title","当前状态":"status","严重度":"severity","提出人":"reporter","处理人":"assignee","创建时间":"created_at"}`
