---
name: test-defect-retrospective
description: 测试缺陷复盘工具包：交互式问卷收集环境信息，解析Jira/禅道缺陷导出文件，自动产出产品/开发/测试三类复盘报告（Markdown+JSON）。当用户要求做版本缺陷复盘、测试总结、缺陷分析或测试效能分析时调用。
---

# 测试缺陷复盘 Skill

纯文件处理的可复用 Skill + 本地预置 Python 解析脚本。无 Web 平台、无服务端、无数据库。
**所有命令默认在本 Skill 目录（本文件所在目录）下执行。** Python 版本要求 3.8+；读取 Excel 需可选依赖 `openpyxl`（CSV 无需任何依赖）。

## 一、启动流程（Skill 加载后立即自动执行）

### 1. 断点检查（断点续跑）

```
python scripts/cli.py session show
```

- 输出中 `answers_complete: true` → 跳过问卷，直接进入第 3 步（目录初始化）。
- 否则按 Q1→Q5 顺序找到第一个未答题目，从该题继续提问（已收集信息不重复问）。

### 2. 交互式问卷（使用 AskUserQuestion 工具；每收到一个答案立即落盘保存）

| 序 | 问题（session key） | 选项 | 分支逻辑 |
|---|---|---|---|
| Q1 | 缺陷管理平台（defect_platform） | Jira / 禅道 / 其他 | 选「其他」→ 追问：① 导出文件格式（CSV/Excel/其他）② 核心字段说明（列名→含义）。据答案将「原始列名→标准字段」写入 `scripts/custom_field_map.json`（字段对照见附录A），然后按 `defect_platform=custom` 保存 |
| Q2 | 需求文档托管平台（req_platform） | Confluence / 企微微盘 / 其他 | 无 |
| Q3 | 需求文件主要格式（req_format） | 纯文本 / 图片 / 流程图 / 在线链接 | 无 |
| Q4 | 测试用例来源与文件格式（testcase_source） | Excel / 平台导出 / 其他 | 无 |
| Q5 | 是否有上一版本复盘报告用于环比（has_prev_report） | 是 / 否 | 选「是」→ 追问上一版本对应目录路径，保存为 `prev_report_path` |

落盘命令示例（每答一题执行一次，中途退出后信息不丢失）：

```
python scripts/cli.py session set --answer defect_platform=jira
python scripts/cli.py session set --answer has_prev_report=true
python scripts/cli.py session set --answer prev_report_path="项目A/v2.3.0"
```

### 3. 目录初始化

向用户确认「项目名」与「版本号」，然后执行：

```
python scripts/cli.py init --root "{项目名}/{版本号}"
```

随后向用户展示以下标准目录结构，引导用户将全部相关文件放入对应子目录（reports/ 由脚本自动写入，用户不放文件）：

```
{项目名}/
└── {版本号}/
    ├── defects/          # 【必填】缺陷平台导出文件（CSV/Excel）
    ├── requirements/     # 【可选】需求文档全量文件
    ├── test_cases/       # 【可选】测试用例文件
    ├── 人员角色.csv      # 【可选】团队角色名单（姓名,角色；角色：前端/后端/产品/测试）
    └── reports/          # 【输出】自动生成的复盘报告
```

**人员角色.csv**（强烈建议提供）：两列 `姓名,角色`。提供后人员效能按真实角色分组——
仅「测试」角色计入测试提报/验证效能（开发自提缺陷剔除并单独标注条数），
仅「前端/后端」角色计入开发修复效能（测试/产品自解决缺陷剔除并标注）；
未登记人员按「未登记」计入并提示补填。未提供时按「提报人=测试、解决人=开发」近似，
并在报告中显式警示角色混入风险。

等待用户确认文件就位后，进入分析流程。

## 二、分析执行流程

### 第 1 步：素材校验（前置校验，必做）

```
python scripts/cli.py scan --root "{项目名}/{版本号}"
```

- 向用户展示「缺失素材清单」（JSON 同时存入 reports/素材校验_{时间戳}.json）。
- `defects/` 为空 → **停止分析**，引导用户补充缺陷导出文件。
- 同时询问用户「当前版本发布时间」（存量遗留缺陷判定基准，格式 `YYYY-MM-DD HH:MM`）。用户无法提供 → 执行 analyze 时不传 `--release-time`，脚本自动以缺陷最大创建时间近似，并在报告中标注口径说明。

### 第 2 步：解析（产出统一标准中间数据集）

```
python scripts/cli.py parse --root "{项目名}/{版本号}" [--platform jira|zentao|custom|auto]
```

- 输出 `reports/中间数据_缺陷标准集_{时间戳}.json`，与源平台解耦。
- `--platform auto`（默认）按表头特征自动识别 Jira / 禅道。
- 若问卷 Q1 选「其他」：使用 `scripts/custom_field_map.json` 映射（custom 解析插件）。

### 第 3 步：分析与产物生成

```
python scripts/cli.py analyze --root "{项目名}/{版本号}" [--release-time "YYYY-MM-DD HH:MM"] [--prev-root 上一版本目录]
```

自动生成（带时间戳，不覆盖历史报告、不修改任何原始输入文件）：

- `reports/复盘报告_产品_{ts}.md` / `复盘报告_产品_{ts}.json`
- `reports/复盘报告_开发_{ts}.md` / `复盘报告_开发_{ts}.json`
- `reports/复盘报告_测试_{ts}.md` / `复盘报告_测试_{ts}.json`
- `reports/复盘数据_指标全量_{ts}.json`（全量指标结构化数据，供工具调用与下版本环比）

版本环比自动匹配：未指定 `--prev-root` 时，在同项目目录下按**版本号语义排序**自动取上一版本，读取其 reports/ 下最新指标 JSON 与报告。

### 第 4 步：AI 定性补充（只追加，不删改脚本产出的量化表格）

脚本完成全部确定性量化计算后，AI 阅读数据与原始文件，向报告对应章节**追加**以下定性内容：

1. Bug 描述语言风格分析与改良建议（依据指标全量 JSON 中各测试人员 `titles_sample` 标题采样）
2. 需求质量四维评估：逻辑自洽性 / 重复赘述 / 文案可读性 / 提示语清晰度（阅读 requirements/ 下文档；在线链接类若无法访问则标注缺失）
3. 产品交互体验合理性评估与优化建议
4. 高风险漏测场景清单：边界 / 并发 / 大数据量 / 弱网（结合脚本输出的模块「缺陷-用例 gap 清单」与关键词扫描结果）
5. 测试环境与生产配置差异风险评估（询问用户提供环境差异信息；不提供则按第六节标注缺失）
6. 自动化优先级清单的接口 / UI 分层建议（依据脚本输出的模块评分表，分层属 AI 建议，评分为确定性计算）
7. 用例设计短板与优化建议、测试流程改进建议、开发侧流程优化建议
8. 可封装成新 Skill 的重复性工作清单
9. 版本环比定性结论：变化点 / 进步点 / 核心短板；上一轮改进项落地效果校验（阅读上一版本报告原文逐项核对）

### 第 5 步：复核

- 确认无任何编造数据与结论（缺失即标注，不推测填充）。
- 确认所有无法分析项已按第六节规则集中标注。
- 向用户报告产物文件路径清单。

## 三、全量分析指标口径（一项不缺；同输入必同输出）

> 详细实现见 `scripts/analyzer.py`。以下口径为唯一标准，AI 不得自行更改。

### 1. 缺陷大盘

| 指标 | 计算口径 |
|---|---|
| Bug总数 | defects/ 导出文件解析去重后的缺陷条目总数 |
| 有效缺陷总数 | Bug总数 − 无效缺陷（解决结果为：不予解决/设计如此/无法重现/无效/重复/转需求） |
| Bug解决率 | 状态组=已关闭（已关闭/已验证通过/Done/Closed）的缺陷数 ÷ 有效缺陷总数 × 100% |
| 严重程度分布 | 按致命/严重/一般/轻微/建议分组统计数量与占比（基于有效缺陷） |
| 业务优先级分布 | 按 P0/P1/P2/P3 分组统计数量与占比 |
| 模块分布 | 各业务模块 Bug 数量与占比 |
| 重复缺陷 | 满足其一：① 平台标记（解决结果=重复 或 含重复关联字段）；② 标题相似度≥80%（指纹去空白标点后 difflib 比对）且同模块且同严重级别 |
| 规律性反复缺陷 | （模块,根因）组合在本次与上一版本均出现（同一模块/同一根因在≥2个版本连续出现）；无上版数据则标注缺失 |
| 偶现缺陷 | 备注/复现概率字段含「难以复现/偶现/偶发/复现率低」，或复现概率标注<30% |
| 存量遗留缺陷 | 创建时间早于当前版本发布时间 且 状态未关闭；发布时间未知则以缺陷最大创建时间近似并标注 |
| 根因统计 | 预置7类关键词归类：需求问题/代码逻辑/环境配置/数据问题/设计缺陷/兼容性问题/操作失误，无法归类记「未分类」 |

### 2. 人员效能

**测试人员维度**（按提报人分组；验证人优先取数据中的验证人字段，缺省取提报人）：

| 指标 | 计算口径 |
|---|---|
| 角色口径 | 提供 `人员角色.csv` 时仅统计角色=测试 的人员（开发/产品自提缺陷剔除并在缺失清单标注条数）；未提供时按全部提报人近似并警示角色混入风险 |
| 提交Bug数量/占比/贡献度 | 个人提报有效缺陷数、占总量百分比 |
| Bug首次验证通过率 | 首次验证即通过（重开次数=0）的缺陷数 ÷ 个人验证总缺陷数 × 100% |
| 驳回重修复率 | 验证不通过、驳回开发重改（重开次数>0）的缺陷数 ÷ 个人验证总缺陷数 × 100% |
| 平均验证时长 | mean(关闭时间 − 开发标记已解决时间)，单位小时 |
| Bug描述语言风格分析与改良建议 | AI 定性（基于个人标题采样，脚本输出 titles_sample） |
| 重复上报Bug率 | 个人上报的重复缺陷数 ÷ 个人上报总数 × 100% |

**开发人员维度**（按解决人，缺省取经办人）：

| 指标 | 计算口径 |
|---|---|
| 角色口径 | 提供 `人员角色.csv` 时仅统计角色=前端/后端 的人员（测试/产品自解决缺陷剔除并标注）；未提供时按全部解决人近似并警示 |
| 平均修复处理时长 | mean(标记已解决时间 − 分配至开发时间；分配时间缺失回退创建时间)，按严重级别分层统计，单位小时 |
| 缺陷回弹率 | 修复后被驳回重改（重开次数>0）的缺陷数 ÷ 个人已修复总缺陷数 × 100% |

### 3. 版本环比（需上一版本报告，缺失则整节标注）

- 与上个版本全量核心指标同比：总数、解决率、重复率、偶现率、根因TOP、平均修复时长、平均验证时长、整体首过率、整体回弹率。
- 校验上一轮复盘提出的改进项落地效果（AI 读上版报告改进项原文 + 本版数据证据）。
- 输出变化点、进步点、核心短板。
- 匹配规则：同项目目录下按版本号语义排序自动取上一版本。

### 4. 需求文档评审（需 requirements/ 需求文档）

- 需求质量评估：逻辑自洽性、重复赘述、文案可读性、提示语清晰度（AI 四维评估）。
- 需求变更统计：变更频次、变更连锁缺陷数量与占比（变更记录文件 `需求变更记录.csv` 存在时脚本统计；否则 AI 定性或标注缺失）。
- 临时加急需求缺陷率 = 缺陷标签/标题含「加急/临时」的缺陷数 ÷ 总缺陷数 × 100%。
- 产品交互体验合理性评估（AI）。
- 缺陷前置溯源：根因=需求问题的缺陷占比。
- 上下游依赖缺失导致的联调缺陷：标题/备注含「联调/依赖/上游/下游/接口对接」的缺陷清单与占比。

### 5. 测试执行指标（需 test_cases/ 用例文件）

| 指标 | 计算口径 |
|---|---|
| 用例总数 | test_cases/ 全部文件解析的用例条目数 |
| 执行状态分布 | 通过/失败/阻塞/跳过/未执行 数量与占比 |
| 用例执行覆盖率 | （通过+失败）÷ 用例总数 × 100% |
| 需求-用例双向覆盖 | ① 缺陷关联需求中被用例覆盖比例；② 用例关联需求完整率；③ 未被用例覆盖的需求清单 |
| 测试阻塞事件统计 | 阻塞用例按原因分类：环境问题/提测延期/依赖/数据/其他 |
| 回归工作量统计 | 回归用例数、回归执行时长合计（用例含「是否回归/执行时长」列时统计） |
| 高风险漏测场景清单 | 关键词扫描（边界/并发/大数据/弱网/断网/超时/峰值）+ 模块缺陷-用例 gap 清单，AI 补充定性 |
| 测试环境与生产配置差异风险评估 | AI 基于用户提供的环境信息评估；无输入则标注缺失 |

### 6. 测试资产优化

| 指标 | 口径 |
|---|---|
| 测试结论与风险提示、后续重点方向 | AI 基于全量数据输出 |
| 用例设计优化建议 | AI 输出 |
| 自动化优先级覆盖清单 | 确定性评分：总分 = 0.3×业务核心度 + 0.2×回归执行频率 + 0.2×手工测试成本 + 0.2×历史缺陷密度 + 0.1×场景稳定性（各维按模块归一化到 0~1；缺用例数据时仅用缺陷维并降权归一）。排序后 P0=前20%、P1=次30%、P2=其余；投入产出评级：总分≥0.6 高 / ≥0.35 中 / 其余低。接口/UI 分层为 AI 建议 |
| 可封装成新Skill的重复性工作清单 | AI 输出 |
| 缺陷全生命周期各阶段耗时分布 | 响应(分配−创建)/修复(解决−分配)/验证(关闭−解决) 平均时长 + 分桶分布（<4h / 4-24h / 1-3天 / >3天） |

## 四、产物归类输出（固定 3 大类，不新增板块，章节不得增减）

所有产物双格式输出：① Markdown 可读报告 ② JSON 结构化数据。章节结构固定如下，脚本生成量化表格，AI 仅在章节内追加定性内容。

### 【产物：产品】复盘报告_产品_{ts}.md

1. 需求文档评审结论
2. 模块缺陷与需求问题关联分析
3. 需求变更统计与变更连锁缺陷分析
4. 临时加急需求质量评估
5. 上下游依赖缺失评估
6. 需求引入类缺陷溯源与占比
7. 产品交互体验评估与优化建议
8. 历史遗留缺陷风险提示

### 【产物：开发】复盘报告_开发_{ts}.md

1. 缺陷大盘全量数据
2. 缺陷根因分类统计
3. 偶现缺陷清单
4. 开发个人修复时效（按严重等级分层）
5. 缺陷回弹率统计
6. 高频重复缺陷根因分析
7. 开发侧流程优化建议

### 【产物：测试】复盘报告_测试_{ts}.md

1. 测试人员提报效能全量指标
2. Bug验证通过率与驳回率统计
3. 版本环比全量分析
4. 上一轮改进项落地效果校验
5. 用例覆盖率与需求-用例双向追踪
6. 测试阻塞事件统计
7. 回归工作量统计
8. 潜在漏测风险清单
9. 测试环境配置差异风险
10. 用例设计短板与优化建议
11. 自动化优先级覆盖清单（接口、UI分层）
12. 可封装Skill工作清单
13. 测试流程改进建议
14. 重复上报缺陷率统计

报告骨架（三类产物统一）：

```markdown
# {项目名} {版本号} 测试缺陷复盘报告 —— {产品|开发|测试}侧

> 生成时间：… ｜ 数据来源：{平台}导出 ｜ 输入文件：…
> 缺失素材：…（无则写"无"）

## 一、{固定章节名}
{脚本量化表格}
{AI定性补充，标记为"【AI分析】"段落}

（章节末尾，如该节数据缺失）
⚠️ 【数据缺失】缺少XX文件/字段，该项暂无法分析
```

## 五、缺失数据处理规则（硬性）

1. **禁止编造**：原始输入文档缺失、关键字段不足时，严禁杜撰数据与结论。
2. **前置校验**：分析启动前先执行 scan 输出「缺失素材清单」，再执行可分析部分。
3. **统一标注**：无法完成的分析项，在对应章节末尾集中标注，格式为：
   `⚠️ 【数据缺失】缺少XX文件/字段，该项暂无法分析`
4. **幂等运行**：重复运行生成带时间戳的报告文件，不覆盖历史报告、不修改任何原始输入文件。

## 六、硬性约束

- 禁止开发任何 Web 平台、服务端、数据库相关代码。
- Skill 加载必须自动触发交互式提问流程。
- 严格遵守 `{项目名}/{版本号}/` 的输入路径规范。
- 所有量化指标口径统一（见第三节），同输入必同输出。
- 不输出人员隐私信息（仅使用缺陷数据中的工作字段），不跨版本合并缺陷数据。
- 最小输入集（仅 defects/）可生成开发、测试类核心产物；产品侧需求评审类章节标注数据缺失。

## 附录A：标准中间数据集字段（解析器输出，与源平台解耦）

| 标准字段 | 含义 | Jira 常见表头 | 禅道常见表头 |
|---|---|---|---|
| bug_id | 缺陷编号 | Issue key / 问题键 | ID / Bug编号 |
| title | 标题 | Summary / 摘要 | Bug标题 |
| status | 状态 | Status / 状态 | 状态 / 当前状态 |
| severity | 严重程度 | Severity / 严重级别 | 严重程度（1-4） |
| priority | 优先级 | Priority / 优先级 | 优先级（1-4） |
| module | 业务模块 | Components / 组件 / 模块 | 模块 / 所属模块 |
| reporter | 提报人 | Reporter / 报告人 | 创建者 |
| assignee | 经办人 | Assignee / 经办人 | 指派给 |
| resolver | 解决人 | Resolver | 由谁解决 / 解决者 |
| verifier | 验证人 | Verifier / 验证人 | 关闭者 |
| created_at | 创建时间 | Created / 创建 | 创建日期 |
| assigned_at | 分配时间 | Assigned / 指派时间 | 指派日期 |
| resolved_at | 解决时间 | Resolved / 已解决 | 解决日期 |
| closed_at | 关闭时间 | Closed / 关闭 | 关闭日期 |
| resolution | 解决结果 | Resolution / 解决结果 | 解决方案 |
| root_cause | 根因 | Root Cause / 根因 | 根因 |
| reopen_count | 重开次数 | Reopen / 重开次数 | 激活次数 |
| labels | 标签 | Labels / 标签 | 关键词 |
| requirement_id | 关联需求 | 需求ID / 关联需求 | 关联需求 |
| repro | 复现概率 | 复现概率 | 复现概率 |
| remark | 备注/描述/复现步骤 | Description / 描述 | 复现步骤 / 备注 |
| duplicate_of | 重复关联 | Duplicate of | 重复关联 |

派生标准字段（脚本计算）：status_group、severity_std、priority_std、resolution_std、root_cause_std、is_occasional、is_duplicate_flag、各时间字段的 *_dt。

## 附录B：CLI 命令参考

```
python scripts/cli.py session show
python scripts/cli.py session set [--project X] [--version Y] [--answer k=v ...]
python scripts/cli.py session reset
python scripts/cli.py init   --root "{项目名}/{版本号}"
python scripts/cli.py scan   --root "{项目名}/{版本号}"
python scripts/cli.py parse  --root "{项目名}/{版本号}" [--platform auto]
python scripts/cli.py analyze --root "{项目名}/{版本号}" [--release-time "YYYY-MM-DD HH:MM"] [--prev-root PATH]
python scripts/cli.py all    --root "{项目名}/{版本号}"   # scan+parse+analyze 串联
```

## 附录C：自定义平台映射文件（Q1 选「其他」时由 AI 生成）

`scripts/custom_field_map.json`：原始表头 → 标准字段（标准字段取值见附录A）。

```json
{
  "工单号": "bug_id",
  "问题主题": "title",
  "当前状态": "status",
  "严重度": "severity",
  "提出人": "reporter",
  "处理人": "assignee",
  "创建时间": "created_at"
}
```
